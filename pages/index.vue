<template>
  <v-main>

<v-container>
    <h1>
      {{test}}
    </h1>
  <p>

    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 coun  네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

      따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
      네, 맞습니다.

      onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.
      t 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
    네, 맞습니다.

    onUpdated 훅은 컴포넌트의 데이터나 상태가 변경되어 컴포넌트가 재렌더링 되면 호출됩니다. 위의 예제에서 increment 함수는 count 값을 증가시키므로, 이 함수를 호출하면 count 값이 변경됩니다. 이 변경으로 인해 컴포넌트는 재렌더링 되고, 재렌더링이 완료된 직후 onUpdated 훅이 호출됩니다.

    따라서 increment 함수를 사용하여 count 값을 증가시키면 "Component is updated!" 라는 메시지가 콘솔에 출력될 것입니다.
  </p>

</v-container>

  </v-main>

</template>

<script setup>

import {ref} from "vue";

const test = ref("test");

</script>
